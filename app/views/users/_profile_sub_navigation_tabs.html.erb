<div id="sub-navigation-tabs">
  <ul>
    <% profile_subnav_tab_links.each do | tab_link | %>
      <li>
        <%= link_to tab_link[:text], tab_link[:url_path],
                    :class => "round #{'selected' if request.path == tab_link[:url_path]}" %>
      </li>
    <% end %>
  </ul>
</div>
